dojo.provide("t.plugins.xy.detailed.test");

if(dojo.isBrowser){
     //Define the HTML file/module URL to import as a 'remote' test.
     doh.registerUrl(
          "t.plugins.xy.detailed.test", 
          dojo.moduleUrl("t", "plugins/xy/detailed/test.html"));
}
